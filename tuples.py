numbers = (1, 2, 3, 4)
# numbers[0] = 2 # not allowed
# print(numbers[0])
# print(numbers.index(4))
# print(numbers.count(1))

# unpacking (can be used with lists, tuples as well)
coords = (1, 2, 3)

x = coords[0]
y = coords[1]
z = coords[2]

a, b, c = coords  # shorthand

print(a, b, c)
